services:
  dashboard:
    build:
      context: .
      dockerfile: infra/docker/dashboard.Dockerfile
    container_name: fulfillment-dashboard
    restart: unless-stopped
    ports:
      - "4173:80"
    volumes:
      - ./apps/dashboard/nginx.frontend-only.conf:/etc/nginx/conf.d/default.conf:ro
